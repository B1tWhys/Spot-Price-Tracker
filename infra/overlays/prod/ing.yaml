apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: spt-fe
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`spotpricetracker.com`, `www.spotpricetracker.com`)"
      services:
        - name: spt-fe
          port: 80
